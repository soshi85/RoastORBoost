# Week 2 Prompt Manual Review — 5 Resumes

بعد از اجرای `tests/run_week2_prompt_stability.py`، فایل `tests/artifacts/week2_prompt_stability_report.json` ساخته می‌شود.

برای هر ۵ Resume این ۴ مورد را بررسی و `TODO` را `PASS` یا `FAIL` کن:
1. Relevance
2. No hallucination
3. Tone
4. Actionability

Prompt Task فقط وقتی Done است که هر ۵ مورد اجرا و Manual Review همگی PASS باشند.
